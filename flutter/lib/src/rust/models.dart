// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.7.0.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import 'frb_generated.dart';
import 'lib.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';
import 'package:uuid/uuid.dart';

// These functions are ignored because they are not marked as `pub`: `get_linestring`, `get_route_polyline`
// These types are ignored because they are not used by any `pub` functions: `AnyAnnotationValue`, `ModelError`, `Route`, `WaypointKind`, `Waypoint`
// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `from`, `from`, `from`, `from`, `from`, `from`, `lower_error`, `lower_error`, `lower_error`, `lower_error`, `lower_error`, `lower_error`, `lower_error`, `lower_error`, `lower_error`, `lower_error`, `lower_error`, `lower_error`, `lower_error`, `lower_error`, `lower_error`, `lower_error`, `lower_error`, `lower_error`, `lower_error`, `lower_error`, `lower_error`, `lower_error`, `lower_return`, `lower_return`, `lower_return`, `lower_return`, `lower_return`, `lower_return`, `lower_return`, `lower_return`, `lower_return`, `lower_return`, `lower_return`, `lower_return`, `lower_return`, `lower_return`, `lower_return`, `lower_return`, `lower_return`, `lower_return`, `lower_return`, `lower_return`, `lower_return`, `lower_return`, `lower`, `lower`, `lower`, `lower`, `lower`, `lower`, `lower`, `lower`, `lower`, `lower`, `lower`, `lower`, `lower`, `lower`, `lower`, `lower`, `lower`, `lower`, `lower`, `lower`, `lower`, `lower`, `lower`, `lower`, `lower`, `lower`, `lower`, `lower`, `lower`, `lower`, `lower`, `lower`, `lower`, `lower`, `lower`, `lower`, `lower`, `lower`, `lower`, `lower`, `lower`, `lower`, `lower`, `lower`, `partial_cmp`, `partial_cmp`, `partial_cmp`, `partial_cmp`, `partial_cmp`, `partial_cmp`, `partial_cmp`, `partial_cmp`, `try_convert_unexpected_callback_error`, `try_convert_unexpected_callback_error`, `try_convert_unexpected_callback_error`, `try_convert_unexpected_callback_error`, `try_convert_unexpected_callback_error`, `try_convert_unexpected_callback_error`, `try_convert_unexpected_callback_error`, `try_convert_unexpected_callback_error`, `try_convert_unexpected_callback_error`, `try_convert_unexpected_callback_error`, `try_convert_unexpected_callback_error`, `try_convert_unexpected_callback_error`, `try_convert_unexpected_callback_error`, `try_convert_unexpected_callback_error`, `try_convert_unexpected_callback_error`, `try_convert_unexpected_callback_error`, `try_convert_unexpected_callback_error`, `try_convert_unexpected_callback_error`, `try_convert_unexpected_callback_error`, `try_convert_unexpected_callback_error`, `try_convert_unexpected_callback_error`, `try_convert_unexpected_callback_error`, `try_lift_successful_return`, `try_lift_successful_return`, `try_lift_successful_return`, `try_lift_successful_return`, `try_lift_successful_return`, `try_lift_successful_return`, `try_lift_successful_return`, `try_lift_successful_return`, `try_lift_successful_return`, `try_lift_successful_return`, `try_lift_successful_return`, `try_lift_successful_return`, `try_lift_successful_return`, `try_lift_successful_return`, `try_lift_successful_return`, `try_lift_successful_return`, `try_lift_successful_return`, `try_lift_successful_return`, `try_lift_successful_return`, `try_lift_successful_return`, `try_lift_successful_return`, `try_lift_successful_return`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_lift`, `try_read`, `try_read`, `try_read`, `try_read`, `try_read`, `try_read`, `try_read`, `try_read`, `try_read`, `try_read`, `try_read`, `try_read`, `try_read`, `try_read`, `try_read`, `try_read`, `try_read`, `try_read`, `try_read`, `try_read`, `try_read`, `try_read`, `try_read`, `try_read`, `try_read`, `try_read`, `try_read`, `try_read`, `try_read`, `try_read`, `try_read`, `try_read`, `try_read`, `try_read`, `try_read`, `try_read`, `try_read`, `try_read`, `try_read`, `try_read`, `try_read`, `try_read`, `try_read`, `try_read`, `write`, `write`, `write`, `write`, `write`, `write`, `write`, `write`, `write`, `write`, `write`, `write`, `write`, `write`, `write`, `write`, `write`, `write`, `write`, `write`, `write`, `write`, `write`, `write`, `write`, `write`, `write`, `write`, `write`, `write`, `write`, `write`, `write`, `write`, `write`, `write`, `write`, `write`, `write`, `write`, `write`, `write`, `write`, `write`
// These functions have error during generation (see debug logs or enable `stop_on_error: true` for more details): `uniffi_ferrostar_fn_func_get_route_polyline`

Future<int> uniffiFerrostarChecksumFuncGetRoutePolyline() =>
    RustLib.instance.api
        .crateModelsUniffiFerrostarChecksumFuncGetRoutePolyline();

// Rust type: RustOpaqueMoi<flutter_rust_bridge::for_generated::RustAutoOpaqueInner<Heading>>
abstract class Heading implements RustOpaqueInterface {
  int get accuracy;

  SystemTime get timestamp;

  int get trueHeading;

  set accuracy(int accuracy);

  set timestamp(SystemTime timestamp);

  set trueHeading(int trueHeading);
}

// Rust type: RustOpaqueMoi<flutter_rust_bridge::for_generated::RustAutoOpaqueInner<SpokenInstruction>>
// abstract class SpokenInstruction implements RustOpaqueInterface {}

// Rust type: RustOpaqueMoi<flutter_rust_bridge::for_generated::RustAutoOpaqueInner<UserLocation>>
abstract class UserLocation implements RustOpaqueInterface {
  GeographicCoordinate get coordinates;

  CourseOverGround? get courseOverGround;

  double get horizontalAccuracy;

  Speed? get speed;

  SystemTime get timestamp;

  set coordinates(GeographicCoordinate coordinates);

  set courseOverGround(CourseOverGround? courseOverGround);

  set horizontalAccuracy(double horizontalAccuracy);

  set speed(Speed? speed);

  set timestamp(SystemTime timestamp);
}

// Rust type: RustOpaqueMoi<flutter_rust_bridge::for_generated::RustAutoOpaqueInner<VisualInstruction>>
// abstract class VisualInstruction implements RustOpaqueInterface {}

/// The lane type blocked by the incident.
enum BlockedLane {
  left,
  leftCenter,
  leftTurnLane,
  center,
  right,
  rightCenter,
  rightTurnLane,
  hov,
  ;
}

/// A geographic bounding box defined by its corners.
class BoundingBox {
  /// The southwest corner of the bounding box.
  final GeographicCoordinate sw;

  /// The northeast corner of the bounding box.
  final GeographicCoordinate ne;

  const BoundingBox({
    required this.sw,
    required this.ne,
  });

  @override
  int get hashCode => sw.hashCode ^ ne.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is BoundingBox &&
          runtimeType == other.runtimeType &&
          sw == other.sw &&
          ne == other.ne;
}

/// Details about congestion for an incident.
class Congestion {
  /// The level of congestion caused by the incident.
  ///
  /// 0 = no congestion
  ///
  /// 100 = road closed
  ///
  /// Other values mean no congestion was calculated
  final int value;

  const Congestion({
    required this.value,
  });

  @override
  int get hashCode => value.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is Congestion &&
          runtimeType == other.runtimeType &&
          value == other.value;
}

/// The direction in which the user/device is observed to be traveling.
class CourseOverGround {
  /// The direction in which the user's device is traveling, measured in clockwise degrees from
  /// true north (N = 0, E = 90, S = 180, W = 270).
  final int degrees;

  /// The accuracy of the course value, measured in degrees.
  final int? accuracy;

  const CourseOverGround({
    required this.degrees,
    this.accuracy,
  });

  // HINT: Make it `#[frb(sync)]` to let it become the default constructor of Dart class.
  /// # Arguments
  ///
  /// - degrees: The direction in which the user's device is traveling, measured in clockwise degrees from
  ///            true north (N = 0, E = 90, S = 180, W = 270).
  ///            NOTE: Input values must lie in the range [0, 360).
  /// - accuracy: the accuracy of the course value, measured in degrees.
  static Future<CourseOverGround> newInstance(
          {required double degrees, int? accuracy}) =>
      RustLib.instance.api
          .crateModelsCourseOverGroundNew(degrees: degrees, accuracy: accuracy);

  @override
  int get hashCode => degrees.hashCode ^ accuracy.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is CourseOverGround &&
          runtimeType == other.runtimeType &&
          degrees == other.degrees &&
          accuracy == other.accuracy;
}

/// A geographic coordinate in WGS84.
class GeographicCoordinate {
  /// The latitude (in degrees).
  final double lat;

  /// The Longitude (in degrees).
  final double lng;

  const GeographicCoordinate({
    required this.lat,
    required this.lng,
  });

  @override
  int get hashCode => lat.hashCode ^ lng.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is GeographicCoordinate &&
          runtimeType == other.runtimeType &&
          lat == other.lat &&
          lng == other.lng;
}

/// The impact of the incident that has occurred.
enum Impact {
  unknown,
  critical,
  major,
  minor,
  low,
  ;
}

/// An incident affecting the free flow of traffic,
/// such as constructions, accidents, and congestion.
class Incident {
  /// A unique identifier for the incident.
  final String id;

  /// The type of incident.
  final IncidentType incidentType;

  /// A short description of the incident.
  final String? description;

  /// A longer description of the incident.
  final String? longDescription;

  /// The time at which the incident was *last* created.
  ///
  /// NB: This can change throughout the life of the incident.
  final DateTime? creationTime;

  /// The time at which the incident started or is expected to start (ex: planned closure).
  final DateTime? startTime;

  /// The time at which the incident ended or is expected to end.
  final DateTime? endTime;

  /// The level of impact to traffic.
  final Impact? impact;

  /// Lanes which are blocked by the incident.
  final List<BlockedLane> lanesBlocked;

  /// Info about the amount of congestion on the road around the incident.
  final Congestion? congestion;

  /// Is the road completely closed?
  final bool? closed;

  /// The index into the [`RouteStep`] geometry where the incident starts.
  final BigInt geometryIndexStart;

  /// The index into the [`RouteStep`] geometry where the incident ends.
  final BigInt? geometryIndexEnd;

  /// Optional additional information about the type of incident (free-form text).
  final String? subType;

  /// Optional descriptions about the type of incident (free-form text).
  final String? subTypeDescription;

  /// The ISO 3166-1 alpha-2 code of the country in which the incident occurs.
  final String? iso31661Alpha2;

  /// The ISO 3166-1 alpha-3 code of the country in which the incident occurs.
  final String? iso31661Alpha3;

  /// A list of road names affected by the incident.
  final List<String> affectedRoadNames;

  /// The bounding box over which the incident occurs.
  final BoundingBox? bbox;

  const Incident({
    required this.id,
    required this.incidentType,
    this.description,
    this.longDescription,
    this.creationTime,
    this.startTime,
    this.endTime,
    this.impact,
    required this.lanesBlocked,
    this.congestion,
    this.closed,
    required this.geometryIndexStart,
    this.geometryIndexEnd,
    this.subType,
    this.subTypeDescription,
    this.iso31661Alpha2,
    this.iso31661Alpha3,
    required this.affectedRoadNames,
    this.bbox,
  });

  @override
  int get hashCode =>
      id.hashCode ^
      incidentType.hashCode ^
      description.hashCode ^
      longDescription.hashCode ^
      creationTime.hashCode ^
      startTime.hashCode ^
      endTime.hashCode ^
      impact.hashCode ^
      lanesBlocked.hashCode ^
      congestion.hashCode ^
      closed.hashCode ^
      geometryIndexStart.hashCode ^
      geometryIndexEnd.hashCode ^
      subType.hashCode ^
      subTypeDescription.hashCode ^
      iso31661Alpha2.hashCode ^
      iso31661Alpha3.hashCode ^
      affectedRoadNames.hashCode ^
      bbox.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is Incident &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          incidentType == other.incidentType &&
          description == other.description &&
          longDescription == other.longDescription &&
          creationTime == other.creationTime &&
          startTime == other.startTime &&
          endTime == other.endTime &&
          impact == other.impact &&
          lanesBlocked == other.lanesBlocked &&
          congestion == other.congestion &&
          closed == other.closed &&
          geometryIndexStart == other.geometryIndexStart &&
          geometryIndexEnd == other.geometryIndexEnd &&
          subType == other.subType &&
          subTypeDescription == other.subTypeDescription &&
          iso31661Alpha2 == other.iso31661Alpha2 &&
          iso31661Alpha3 == other.iso31661Alpha3 &&
          affectedRoadNames == other.affectedRoadNames &&
          bbox == other.bbox;
}

/// The type of incident that has occurred.
enum IncidentType {
  accident,
  congestion,
  construction,
  disabledVehicle,
  laneRestriction,
  massTransit,
  miscellaneous,
  otherNews,
  plannedEvent,
  roadClosure,
  roadHazard,
  weather,
  ;
}

/// The content of a visual instruction.
class LaneInfo {
  final bool active;
  final List<String> directions;
  final String? activeDirection;

  const LaneInfo({
    required this.active,
    required this.directions,
    this.activeDirection,
  });

  @override
  int get hashCode =>
      active.hashCode ^ directions.hashCode ^ activeDirection.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is LaneInfo &&
          runtimeType == other.runtimeType &&
          active == other.active &&
          directions == other.directions &&
          activeDirection == other.activeDirection;
}

/// Additional information to further specify a [`ManeuverType`].
enum ManeuverModifier {
  uTurn,
  sharpRight,
  right,
  slightRight,
  straight,
  slightLeft,
  left,
  sharpLeft,
  ;
}

/// The broad class of maneuver to perform.
///
/// This is usually combined with [`ManeuverModifier`] in [`VisualInstructionContent`].
enum ManeuverType {
  turn,
  newName,
  depart,
  arrive,
  merge,
  onRamp,
  offRamp,
  fork,
  endOfRoad,
  continue_,
  roundabout,
  rotary,
  roundaboutTurn,
  notification,
  exitRoundabout,
  exitRotary,
  ;
}

/// A maneuver (such as a turn or merge) followed by travel of a certain distance until reaching
/// the next step.
class RouteStep {
  /// The full route geometry for this step.
  final List<GeographicCoordinate> geometry;

  /// The distance, in meters, to travel along the route after the maneuver to reach the next step.
  final double distance;

  /// The estimated duration, in seconds, that it will take to complete this step.
  final double duration;

  /// The name of the road being traveled on (useful for certain UI styles).
  final String? roadName;

  /// A list of exits (name or number).
  final List<String> exits;

  /// A description of the maneuver (ex: "Turn wright onto main street").
  ///
  /// Note for UI implementers: the context this appears in (or doesn't)
  /// depends somewhat on your use case and routing engine.
  /// For example, this field is useful as a written instruction in Valhalla.
  final String instruction;

  /// A list of instructions for visual display (usually as banners) at specific points along the step.
  final List<VisualInstruction> visualInstructions;

  /// A list of prompts to announce (via speech synthesis) at specific points along the step.
  final List<SpokenInstruction> spokenInstructions;

  /// A list of json encoded strings representing annotations between each coordinate along the step.
  final List<String>? annotations;

  /// A list of incidents that occur along the step.
  final List<Incident> incidents;

  const RouteStep({
    required this.geometry,
    required this.distance,
    required this.duration,
    this.roadName,
    required this.exits,
    required this.instruction,
    required this.visualInstructions,
    required this.spokenInstructions,
    this.annotations,
    required this.incidents,
  });

  /// Gets the active visual instruction at a specific point along the step.
  Future<VisualInstruction?> getActiveVisualInstruction(
          {required double distanceToEndOfStep}) =>
      RustLib.instance.api.crateModelsRouteStepGetActiveVisualInstruction(
          that: this, distanceToEndOfStep: distanceToEndOfStep);

  /// Get the annotation data at a specific point along the step.
  ///
  /// `at_coordinate_index` is the index of the coordinate in the step geometry.
  Future<String?> getAnnotationAtCurrentIndex(
          {required BigInt atCoordinateIndex}) =>
      RustLib.instance.api.crateModelsRouteStepGetAnnotationAtCurrentIndex(
          that: this, atCoordinateIndex: atCoordinateIndex);

  /// Gets the spoken instruction at a specific point along the step.
  ///
  /// Note to platform implementers: some care is needed with this.
  /// Unlike visual instructions, which can be changed without much consequence,
  /// speech synthesis takes time to complete.
  /// Take care to characteristics of your synthesis engine,
  /// including whether utterances are queued or cut off the currently playing one.
  /// You will also need some sort of check to ensure you don't make the same announcement
  /// more times than necessary.
  Future<SpokenInstruction?> getCurrentSpokenInstruction(
          {required double distanceToEndOfStep}) =>
      RustLib.instance.api.crateModelsRouteStepGetCurrentSpokenInstruction(
          that: this, distanceToEndOfStep: distanceToEndOfStep);

  @override
  int get hashCode =>
      geometry.hashCode ^
      distance.hashCode ^
      duration.hashCode ^
      roadName.hashCode ^
      exits.hashCode ^
      instruction.hashCode ^
      visualInstructions.hashCode ^
      spokenInstructions.hashCode ^
      annotations.hashCode ^
      incidents.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is RouteStep &&
          runtimeType == other.runtimeType &&
          geometry == other.geometry &&
          distance == other.distance &&
          duration == other.duration &&
          roadName == other.roadName &&
          exits == other.exits &&
          instruction == other.instruction &&
          visualInstructions == other.visualInstructions &&
          spokenInstructions == other.spokenInstructions &&
          annotations == other.annotations &&
          incidents == other.incidents;
}

/// The speed of the user from the location provider.
class Speed {
  /// The user's speed in meters per second.
  final double value;

  /// The accuracy of the speed value, measured in meters per second.
  final double? accuracy;

  const Speed({
    required this.value,
    this.accuracy,
  });

  @override
  int get hashCode => value.hashCode ^ accuracy.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is Speed &&
          runtimeType == other.runtimeType &&
          value == other.value &&
          accuracy == other.accuracy;
}

/// An instruction that can be synthesized using a TTS engine to announce an upcoming maneuver.
///
/// Note that these do not have any locale information attached.
class SpokenInstruction {
  /// Plain-text instruction which can be synthesized with a TTS engine.
  final String text;

  /// Speech Synthesis Markup Language, which should be preferred by clients capable of understanding it.
  final String? ssml;

  /// How far (in meters) from the upcoming maneuver the instruction should start being displayed
  final double triggerDistanceBeforeManeuver;

  /// A unique identifier for this instruction.
  ///
  /// This is provided so that platform-layer integrations can easily disambiguate between distinct utterances,
  /// which may have the same textual content.
  /// UUIDs conveniently fill this purpose.
  ///
  /// NOTE: While it is possible to deterministically create UUIDs, we do not do so at this time.
  /// This should be theoretically possible though if someone cares to write up a proposal and a PR.
  final UuidValue utteranceId;

  const SpokenInstruction({
    required this.text,
    this.ssml,
    required this.triggerDistanceBeforeManeuver,
    required this.utteranceId,
  });

  @override
  int get hashCode =>
      text.hashCode ^
      ssml.hashCode ^
      triggerDistanceBeforeManeuver.hashCode ^
      utteranceId.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is SpokenInstruction &&
          runtimeType == other.runtimeType &&
          text == other.text &&
          ssml == other.ssml &&
          triggerDistanceBeforeManeuver ==
              other.triggerDistanceBeforeManeuver &&
          utteranceId == other.utteranceId;
}

/// An instruction for visual display (usually as banners) at a specific point along a [`RouteStep`].
class VisualInstruction {
  /// The primary instruction content.
  ///
  /// This is usually given more visual weight.
  final VisualInstructionContent primaryContent;

  /// Optional secondary instruction content.
  final VisualInstructionContent? secondaryContent;

  /// Optional sub-maneuver instruction content.
  final VisualInstructionContent? subContent;

  /// How far (in meters) from the upcoming maneuver the instruction should start being displayed
  final double triggerDistanceBeforeManeuver;

  const VisualInstruction({
    required this.primaryContent,
    this.secondaryContent,
    this.subContent,
    required this.triggerDistanceBeforeManeuver,
  });

  @override
  int get hashCode =>
      primaryContent.hashCode ^
      secondaryContent.hashCode ^
      subContent.hashCode ^
      triggerDistanceBeforeManeuver.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is VisualInstruction &&
          runtimeType == other.runtimeType &&
          primaryContent == other.primaryContent &&
          secondaryContent == other.secondaryContent &&
          subContent == other.subContent &&
          triggerDistanceBeforeManeuver == other.triggerDistanceBeforeManeuver;
}

/// The content of a visual instruction.
class VisualInstructionContent {
  /// The text to display.
  final String text;

  /// A standardized maneuver type (if any).
  final ManeuverType? maneuverType;

  /// A standardized maneuver modifier (if any).
  final ManeuverModifier? maneuverModifier;

  /// If applicable, the number of degrees you need to go around the roundabout before exiting.
  ///
  /// For example, entering and exiting the roundabout in the same direction of travel
  /// (as if you had gone straight, apart from the detour)
  /// would be an exit angle of 180 degrees.
  final int? roundaboutExitDegrees;

  /// Detailed information about the lanes. This is typically only present in sub-maneuver instructions.
  final List<LaneInfo>? laneInfo;

  /// The exit number (or similar identifier like "8B").
  final List<String> exitNumbers;

  const VisualInstructionContent({
    required this.text,
    this.maneuverType,
    this.maneuverModifier,
    this.roundaboutExitDegrees,
    this.laneInfo,
    required this.exitNumbers,
  });

  @override
  int get hashCode =>
      text.hashCode ^
      maneuverType.hashCode ^
      maneuverModifier.hashCode ^
      roundaboutExitDegrees.hashCode ^
      laneInfo.hashCode ^
      exitNumbers.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is VisualInstructionContent &&
          runtimeType == other.runtimeType &&
          text == other.text &&
          maneuverType == other.maneuverType &&
          maneuverModifier == other.maneuverModifier &&
          roundaboutExitDegrees == other.roundaboutExitDegrees &&
          laneInfo == other.laneInfo &&
          exitNumbers == other.exitNumbers;
}
